--- OldCarUserControl.cs	2016-07-26 06:28:58.000000000 +0900
+++ CarUserControl.cs	2016-07-24 08:28:47.000000000 +0900
@@ -23,8 +23,24 @@
             float h = CrossPlatformInputManager.GetAxis("Horizontal");
             float v = CrossPlatformInputManager.GetAxis("Vertical");
 #if !MOBILE_INPUT
-            float handbrake = CrossPlatformInputManager.GetAxis("Jump");
-            m_Car.Move(h, v, v, handbrake);
+            float b = CrossPlatformInputManager.GetAxis("Jump");
+            if(b == 0) {
+                if(v > 0) {
+                    m_Car.Move(h, v, v, 0);
+                }
+				else {
+					m_Car.Move(h, 0, 0, 0);
+				}
+            }
+            else {
+                if(v < 0) {
+                    m_Car.Move(h, v, v, 0);
+                }
+                else {
+                    m_Car.Move(h, 0, 0, b);
+                }
+            }
+
 #else
             m_Car.Move(h, v, v, 0f);
 #endif
